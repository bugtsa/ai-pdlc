# Architecture Intent

The fixture package is intentionally small. It only exists to verify that the MCP server can resolve, validate, and summarize a package from a standalone repo-local fixture root.
